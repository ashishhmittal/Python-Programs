git config --global user.name "ashishhmittal"

git config --global user.email "amhmittal@gmail.com"

git init

git add .

git status

git commit -m "Message To Be Displayed"

git remote add origin 

git push origin master